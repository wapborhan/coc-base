import Banner from "@/components/banner/Banner";
import React from "react";

const page = async () => {
  return (
    <main className="w-full">
      <Banner />
    </main>
  );
};

export default page;
